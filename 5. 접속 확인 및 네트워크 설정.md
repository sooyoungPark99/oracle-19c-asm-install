# 접속 확인 및 네트워크 설정

---

### 1. ASM / DB 인스턴스 기동 확인

pmon 프로세스를 확인하여 ASM과 DB 인스턴스가 정상 기동됐는지 확인함.

```bash
ps -ef | grep pmon
```

기대 출력:

```
oracle    3146     1  0 15:43 ?   00:00:00 asm_pmon_+ASM
oracle    3348     1  0 15:43 ?   00:00:00 ora_pmon_orcl
```

> `asm_pmon_+ASM` 과 `ora_pmon_orcl` 이 모두 떠 있어야 정상

.bash_profile 환경변수를 확인

```bash
cat .bash_profile
```

주요 환경변수 설정 내용

```bash
export ORACLE_HOSTNAME=asmtest
export ORACLE_UNQNAME=orcl
export ORACLE_BASE=/app/oracle
export ORACLE_HOME=$ORACLE_BASE/product/19.0.0/db_1
export GRID_HOME=/app/grid
export ORA_INVENTORY=/oralnventory
export ORACLE_SID=orcl

export PATH=/usr/sbin:/usr/local/bin:$PATH
export PATH=$ORACLE_HOME/bin:$PATH

export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export CLASSPATH=$ORACLE_HOME/jlib:$ORACLE_HOME/rdbms/jlib

export CV_ASSUME_DISTID=RHEL7.6

export LANG=C
export LC_ALL=C

alias grid_env='export ORACLE_HOME=$GRID_HOME; export ORACLE_SID=+ASM; export PATH=$GRID_HOME/bin:$PATH'
alias db_env='export ORACLE_HOME=$ORACLE_BASE/product/19.0.0/db_1; export ORACLE_SID=orcl'
alias ss='sqlplus / as sysdba'
alias oh='cd $ORACLE_HOME'
alias gh='cd $GRID_HOME'
```

---

### 2. SYS로 DB 접속 확인

oraenv로 환경변수를 설정함.

```bash
. oraenv
# ORACLE_SID = [orcl] ? orcl
```

SYS로 접속하여 인스턴스 상태를 확인.

```bash
sqlplus sys/oracle as sysdba
```

```sql
select instance_name, status from v$instance;
```

기대 출력:

```
INSTANCE_NAME    STATUS
---------------- ------------
orcl             OPEN
```

리스너 상태를 확인

```bash
lsnrctl status
```

> +ASM, +ASM_CRS, +ASM_DATA, +ASM_RECO, orcl 서비스가 모두 READY 상태여야 정상임.

---

### 3. listener.ora HOST 수정

listener.ora의 HOST를 호스트명에서 실제 IP로 변경함.

```bash
cd /app/grid/network/admin/
vi listener.ora
```

아래와 같이 HOST를 실제 IP로 수정 진행.

```
LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = 192.168.23.67)(PORT = 1521))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
    )
  )

ENABLE_GLOBAL_DYNAMIC_ENDPOINT_LISTENER=ON
VALID_NODE_CHECKING_REGISTRATION_LISTENER=ON
```

수정한 listener.ora를 ORACLE_HOME 경로에도 복사함.

```bash
cp listener.ora $ORACLE_HOME/network/admin/
```

리스너를 재기동 진행.

```bash
lsnrctl stop
lsnrctl start
lsnrctl status
```

IP로 원격 접속 테스트

```bash
sqlplus sys/oracle@192.168.23.67:1521/orcl as sysdba
```

> 접속 성공 시 `Connected to: Oracle Database 19c Enterprise Edition` 메시지가 출력됨.

---

### 4. 방화벽 1521/tcp 포트 오픈

root 유저로 전환하여 방화벽을 설정한다.

```bash
su -
```

```bash
firewall-cmd --permanent --add-port=1521/tcp
firewall-cmd --reload

# 포트 오픈 확인
netstat -tlnp | grep 1521
```

기대 출력:

```

tcp   0   0 192.168.23.67:1521   0.0.0.0:*   LISTEN   4526/tnslsnr
```


