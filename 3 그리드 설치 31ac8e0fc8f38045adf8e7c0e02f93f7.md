# 3. 그리드 설치

### 1. /home/oracle 내 리눅스 파일 압축 해제

```sql
[oracle@asmtest ~]$ ls -l *.zip
-rw-r--r--. 1 oracle oinstall 3059705302  3월  4 16:29 LINUX.X64_193000_db_home.    zip
-rw-r--r--. 1 oracle oinstall 2889184573  3월  4 16:31 LINUX.X64_193000_grid_hom    e.zip
[oracle@asmtest ~]$
[oracle@asmtest ~]$ mv LINUX.X64_193000_grid_home.zip /app/grid/
[oracle@asmtest ~]$
[oracle@asmtest ~]$ cd /app/grid
[oracle@asmtest grid]$ ls
LINUX.X64_193000_grid_home.zip
[oracle@asmtest grid]$
[oracle@asmtest grid]$ unzip LINUX.X64_193000_grid_home.zip

[oracle@asmtest grid]$ ls
LINUX.X64_193000_grid_home.zip  evm            oracore         runcluvfy.sh
OPatch                          gpnp           ord             sdk
QOpatch                         gridSetup.sh   ords            slax
addnode                         has            oss             sqlpatch
assistants                      hs             oui             sqlplus
bin                             install        owm             srvm
cha                             instantclient  perl            suptools
clone                           inventory      plsql           tomcat
crs                             javavm         precomp         ucp
css                             jdbc           qos             usm
cv                              jdk            racg            utl
dbjava                          jlib           rdbms           welcome.html
dbs                             ldap           relnotes        wlm
deinstall                       lib            rhp             wwg
demo                            md             root.sh         xag
diagnostics                     network        root.sh.old     xdk
dmu                             nls            root.sh.old.1
env.ora                         opmn           rootupgrade.sh
[oracle@asmtest grid]$

```

### 2. .bash_profile 생성

#### oracle 유저에서

- **.bash_profile 내용**

```sql
# .bash_profile
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

PATH=$PATH:$HOME/.local/bin:$HOME/bin

export PATH
export TMP=/tmp
export TMPDIR=$TMP

export ORACLE_HOSTNAME=psyasm
export ORACLE_UNQNAME=orcl
export ORACLE_BASE=/app/oracle
export ORACLE_HOME=$ORACLE_BASE/product/19.0.0/db_1
export GRID_HOME=/app/grid
export ORA_INVENTORY=/oralnventory
export ORACLE_SID=orcl

# 공백 제거 + ORACLE_HOME/bin 은 PATH 앞에 추가 (권장)
export PATH=/usr/sbin:/usr/local/bin:$PATH
export PATH=$ORACLE_HOME/bin:$PATH

export LD_LIBRARY_PATH=$ORACLE_HOME/lib:/lib:/usr/lib
export CLASSPATH=$ORACLE_HOME/jlib:$ORACLE_HOME/rdbms/jlib

export CV_ASSUME_DISTID=RHEL7.6

export LANG=C
export LC_ALL=C

alias grid_env='export ORACLE_HOME=$GRID_HOME; export ORACLE_SID=+ASM; export PATH=$GRID_HOME/bin:$PATH'
alias db_env='export ORACLE_HOME=$ORACLE_BASE/product/19.0.0/db_1; export ORACLE_SID=orcl'
alias ss='sqlplus / as sysdba'
alias oh='cd $ORACLE_HOME'
alias gh='cd $GRID_HOME'

```

```sql
[oracle@asmtest grid]$ cd
[oracle@asmtest ~]$
[oracle@asmtest ~]$ vi .bash_profile
```

#### root에서

- **.bash_profile 내용**

```sql
# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# User specific environment and startup programs

PATH=$PATH:$HOME/bin

export ORACLE_BASE=/app/oracle
export GRID_HOME=/app/grid
export ORACLE_HOME=$ORACLE_BASE/product/19c/db_1
export PATH=$PATH:$GRID_HOME/bin
```

```sql
[oracle@psyasm ~]$ su -
암호:
마지막 로그인: 수  3월  4 16:16:18 KST 2026 일시 pts/1
[root@psyasm ~]#
[root@psyasm ~]#
[root@psyasm ~]# pwd
/root
[root@psyasm ~]#
[root@psyasm ~]# vi .bash_profile

-- .bash_profile을 다음과 같이 source 로 수행합니다.
[root@psyasm ~]# source .bash_profile
[root@psyasm ~]#
```

### 3. ASMlib 사전 설정(oracleasm)

```sql
[root@psyasm ~]# oracleasm configure -i
Configuring the Oracle ASM library driver.

This will configure the on-boot properties of the Oracle ASM library
driver.  The following questions will determine whether the driver is
loaded on boot and what permissions it will have.  The current values
will be shown in brackets ('[]').  Hitting <ENTER> without typing an
answer will keep that current value.  Ctrl-C will abort.

Default user to own the driver interface []: oracle
Default group to own the driver interface []: asmadmin
Start Oracle ASM library driver on boot (y/n) [n]: y
Scan for Oracle ASM disks on boot (y/n) [y]: y
Writing Oracle ASM library driver configuration: done
[root@psyasm ~]#
[root@psyasm ~]# oracleasm init
Creating /dev/oracleasm mount point: /dev/oracleasm
Loading module "oracleasm": oracleasm
Configuring "oracleasm" to use device physical block size
Mounting ASMlib driver filesystem: /dev/oracleasm
[root@psyasm ~]#

[root@psyasm ~]# oracleasm status
Checking if ASM is loaded: yes
Checking if /dev/oracleasm is mounted: yes
[root@psyasm ~]#
[root@psyasm ~]# oracleasm configure
ORACLEASM_ENABLED=true
ORACLEASM_UID=oracle
ORACLEASM_GID=asmadmin
ORACLEASM_SCANBOOT=true
ORACLEASM_SCANORDER=""
ORACLEASM_SCANEXCLUDE=""
ORACLEASM_SCAN_DIRECTORIES=""
ORACLEASM_USE_LOGICAL_BLOCK_SIZE="false"
[root@psyasm ~]#
```

### 4. 디스크 장치 확인

```sql
[root@psyasm ~]# ls /dev/sd*
/dev/sda  /dev/sda1  /dev/sda2

```

### 5. 가상 디스크 추가(VM 설정)

- **crs1,2,3** : asm 파일용, 1G
- **data** : database 용, 최소 20G
- **reco** : recovery 영역 20G

아래 이미지 순서대로 디스크를 추가했다.

![image.png](image.png)

![image.png](image%201.png)

![image.png](image%202.png)

1. **crs 1,2,3 디스크** 

![image.png](image%203.png)

![image.png](image%204.png)

![image.png](image%205.png)

![image.png](image%206.png)

1. **data 디스크**

![image.png](image%207.png)

1. **reco 디스크** 

![image.png](image%208.png)

**최종 생성 확인**

![image.png](image%209.png)

![image.png](image%2010.png)

### 6. 리눅스 시작

![image.png](image%2011.png)

### 7. fdisk로 파티션 생성(root)

![image.png](image%2012.png)

![image.png](image%2013.png)

```sql
[root@psyasm ~]# ls /dev/sd*
/dev/sda   /dev/sda2  /dev/sdc  /dev/sde
/dev/sda1  /dev/sdb   /dev/sdd  /dev/sdf

[root@psyasm ~]# fdisk /dev/sdb
Welcome to fdisk (util-linux 2.23.2).

Changes will remain in memory only, until you decide to write them.
Be careful before using the write command.

Device does not contain a recognized partition table
Building a new DOS disklabel with disk identifier 0x886509d1.

Command (m for help): n
Partition type:
   p   primary (0 primary, 0 extended, 4 free)
   e   extended
Select (default p): p
Partition number (1-4, default 1): 1
First sector (2048-2097151, default 2048): 그냥 엔터
Using default value 2048
Last sector, +sectors or +size{K,M,G} (2048-2097151, default 2097151): 그냥 엔터
Using default value 2097151
Partition 1 of type Linux and of size 1023 MiB is set

Command (m for help): w
The partition table has been altered!

Calling ioctl() to re-read partition table.
Syncing disks.

[root@psyasm ~]# fdisk /dev/sdc
Welcome to fdisk (util-linux 2.23.2).

Changes will remain in memory only, until you decide to write them.
Be careful before using the write command.

Device does not contain a recognized partition table
Building a new DOS disklabel with disk identifier 0x26b82ba7.

Command (m for help): n
Partition type:
   p   primary (0 primary, 0 extended, 4 free)
   e   extended
Select (default p): p
Partition number (1-4, default 1): 1
First sector (2048-2097151, default 2048): 그냥 엔터
Using default value 2048
Last sector, +sectors or +size{K,M,G} (2048-2097151, default 2097151): 그냥 엔터
Using default value 2097151
Partition 1 of type Linux and of size 1023 MiB is set

Command (m for help): w
The partition table has been altered!

Calling ioctl() to re-read partition table.
Syncing disks.

-- 그 다음 아래의 것도 반복해준다.

  fdisk /dev/sde
  fdisk /dev/sdf
```

### 8. 파티션 생성 결과 확인

![image.png](image%2014.png)

#### 위와 같이 13개가 아닌 12개로 잘 못 생성되었을 경우,  fdisk /dev/sdb 삭제 후 다시 작업 필요하다.

```sql

[root@psyasm ~]# ls /dev/sd*
/dev/sda   /dev/sda2  /dev/sdb1  /dev/sdc1  /dev/sde   /dev/sdf
/dev/sda1  /dev/sdb   /dev/sdc   /dev/sdd   /dev/sde1  /dev/sdf1
[root@psyasm ~]# ls /dev/sd* |wc -l
12
[root@psyasm ~]#
[root@psyasm ~]# fdisk /dev/sdb
Welcome to fdisk (util-linux 2.23.2).

Changes will remain in memory only, until you decide to write them.
Be careful before using the write command.

Command (m for help): ^Z
[2]+  Stopped                 fdisk /dev/sdb
[root@psyasm ~]#
[root@psyasm ~]# fdisk /dev/sdb
Welcome to fdisk (util-linux 2.23.2).

Changes will remain in memory only, until you decide to write them.
Be careful before using the write command.

Command (m for help): p

Disk /dev/sdb: 1073 MB, 1073741824 bytes, 2097152 sectors
Units = sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes
Disk label type: dos
Disk identifier: 0x886509d1

   Device Boot      Start         End      Blocks   Id  System
/dev/sdb1            2048     2097151     1047552   83  Linux

Command (m for help): d
Selected partition 1
Partition 1 is deleted

Command (m for help): n
Partition type:
   p   primary (0 primary, 0 extended, 4 free)
   e   extended
Select (default p): p
Partition number (1-4, default 1): 1
First sector (2048-2097151, default 2048):
Using default value 2048
Last sector, +sectors or +size{K,M,G} (2048-2097151, default 2097151):
Using default value 2097151
Partition 1 of type Linux and of size 1023 MiB is set

Command (m for help): w
The partition table has been altered!

Calling ioctl() to re-read partition table.
Syncing disks.

```

#### **정상 생성 화면**

![image.png](image%2015.png)

### 9. ASM 디스크 생성(oracleasm)

```sql
[root@psyasm ~]# oracleasm createdisk crs1 /dev/sdb1
Writing disk header: done
Instantiating disk: done
[root@psyasm ~]# oracleasm createdisk crs2 /dev/sdc1
Writing disk header: done
Instantiating disk: done
[root@psyasm ~]# oracleasm createdisk crs3 /dev/sdd1
Writing disk header: done
Instantiating disk: done
[root@psyasm ~]# oracleasm createdisk data /dev/sde1
Writing disk header: done
Instantiating disk: done
[root@psyasm ~]# oracleasm createdisk reco /dev/sdf1
Writing disk header: done
Instantiating disk: done
[root@psyasm ~]# oracleasm scandisks
Reloading disk partitions: done
Cleaning any stale ASM disks...
Scanning system for ASM disks...
[root@psyasm ~]# oracleasm listdisks
CRS1
CRS2
CRS3
DATA
RECO
```

### 10. [gridSetup.sh](http://gridSetup.sh) 실행(oracle)

```sql
Last login: Thu Mar  5 18:08:51 2026
[oracle@psyasm ~]$
[oracle@psyasm ~]$ echo $DISPLAY
localhost:10.0      <-- localhost 버전 확인 必
[oracle@psyasm ~]$
[oracle@psyasm ~]$ cd /app/grid
[oracle@psyasm grid]$
[oracle@psyasm grid]$ ls
LINUX.X64_193000_grid_home.zip  evm            oracore         runcluvfy.sh
OPatch                          gpnp           ord             sdk
QOpatch                         gridSetup.sh   ords            slax
addnode                         has            oss             sqlpatch
assistants                      hs             oui             sqlplus
bin                             install        owm             srvm
cha                             instantclient  perl            suptools
clone                           inventory      plsql           tomcat
crs                             javavm         precomp         ucp
css                             jdbc           qos             usm
cv                              jdk            racg            utl
dbjava                          jlib           rdbms           welcome.html
dbs                             ldap           relnotes        wlm
deinstall                       lib            rhp             wwg
demo                            md             root.sh         xag
diagnostics                     network        root.sh.old     xdk
dmu                             nls            root.sh.old.1
env.ora                         opmn           rootupgrade.sh
[oracle@psyasm grid]$ ./gridSetup.sh
Launching Oracle Grid Infrastructure Setup Wizard...
```

아래 이미지 순서대로 Grid 설치를 진행한다.

![image.png](image%2016.png)

![image.png](image%2017.png)

![image.png](image%2018.png)

- **password : oracle**

![image.png](image%2019.png)

![image.png](image%2020.png)

![image.png](image%2021.png)

![image.png](image%2022.png)

![image.png](image%2023.png)

![image.png](image%2024.png)

![image.png](image%2025.png)

- **root password :  oracle**

![image.png](image%2026.png)

**아래와 같은 화면이 뜨면**  
**1) Fix & Check Again** 진행  해결되지 않는 항목들은  **2)** **ignore all** 선택

![image.png](image%2027.png)

![image.png](image%2028.png)

![image.png](image%2029.png)

![image.png](image%2030.png)

![image.png](image%2031.png)

**아래 화면이 뜨면 설치 완료 !**

![image.png](image%2032.png)

### 11. ASMCA로 디스크 그룹 생성

- ASM Diskgroup 3가지
1. CRS  :   GRID 설치
2. DATA : 20기가  : DB 설치  
3. RECO : 20기가  : 데이터 백업 

```sql
[oracle@psyasm ~]$ grid_env
[oracle@psyasm ~]$ asmca
```

![image.png](image%2033.png)

![image.png](image%2034.png)

![image.png](image%2035.png)

![image.png](image%2036.png)

### 아래 코드 수행하여 확인

```sql
[oracle@psyasm ~]$ crsctl stat res -t
--------------------------------------------------------------------------------
Name           Target  State        Server                   State details
--------------------------------------------------------------------------------
Local Resources
--------------------------------------------------------------------------------
ora.CRS.dg
               ONLINE  ONLINE       psyasm                   STABLE
ora.DATA.dg
               ONLINE  ONLINE       psyasm                   STABLE
ora.LISTENER.lsnr
               ONLINE  ONLINE       psyasm                   STABLE
ora.RECO.dg
               ONLINE  ONLINE       psyasm                   STABLE
ora.asm
               ONLINE  ONLINE       psyasm                   Started,STABLE
ora.ons
               OFFLINE OFFLINE      psyasm                   STABLE
--------------------------------------------------------------------------------
Cluster Resources
--------------------------------------------------------------------------------
ora.cssd
      1        ONLINE  ONLINE       psyasm                   STABLE
ora.diskmon
      1        OFFLINE OFFLINE                               STABLE
ora.evmd
      1        ONLINE  ONLINE       psyasm                   STABLE
--------------------------------------------------------------------------------
[oracle@psyasm ~]$

```